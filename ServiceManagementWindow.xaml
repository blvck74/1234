<Window x:Class="WpfLb1.ServiceManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Управление сервисами" Height="450" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель добавления сервиса -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBox x:Name="ServiceNameTextBox"
                     materialDesign:HintAssist.Hint="Название сервиса"
                     Margin="0,0,0,10"/>
            
            <Button Content="Добавить сервис"
                    Click="OnAddService_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>

        <!-- Список сервисов -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ServicesList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Text="{Binding}" 
                                     VerticalAlignment="Center"/>
                            
                            <Button Grid.Column="1" 
                                    Content="Удалить"
                                    Click="OnDeleteService_Click"
                                    Tag="{Binding}"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Margin="10,0,0,0"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
